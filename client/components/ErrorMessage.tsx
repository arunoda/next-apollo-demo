import React from 'react';
import { Typography } from '@mui/material';

export const ErrorMessage = () => (
  <Typography fontSize="20px" color="red" fontFamily="Roboto" variant="h1" textAlign="center" fontWeight={700}>
    FETCH ERROR
  </Typography>
);
